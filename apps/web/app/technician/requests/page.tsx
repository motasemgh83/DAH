'use client'; import { useEffect, useState } from 'react'; import { getToken } from '@/lib/auth'; import { apiFetch } from '@/lib/api'; export default function TechnicianRequestsPage(){ const [rows,setRows]=useState<any[]>([]); useEffect(()=>{ apiFetch('/requests',{},getToken()).then((r)=>setRows(r.data)); },[]); return <main className="rtl"><h1>الطلبات</h1><table className="table"><thead><tr><th>رقم الطلب</th><th>العنوان</th><th>الحالة</th><th>الأولوية</th></tr></thead><tbody>{rows.map((item)=><tr key={item.id}><td>{item.requestNo}</td><td>{item.title}</td><td>{item.status}</td><td>{item.priority}</td></tr>)}</tbody></table></main>; }
