'use client'; import { useEffect, useState } from 'react'; import { apiFetch } from '@/lib/api'; import { getToken } from '@/lib/auth'; import { DataTable } from '@/components/DataTable'; export default function PreventivePage(){ const [rows,setRows]=useState<any[]>([]); useEffect(()=>{ apiFetch('/assets/preventive-plans',{},getToken()).then((r)=>setRows(r.data)); },[]); return <main><h1>Preventive Maintenance Plans</h1><DataTable columns={['Plan','Asset','Frequency (days)','Next Due','Checklist']} rows={rows.map((item)=>[item.title,item.asset?.name || '-',item.frequencyDays,new Date(item.nextDueAt).toLocaleDateString(),Array.isArray(item.checklistJson)?item.checklistJson.join(', '):'-'])} /></main>; }
