'use client'; import { useEffect, useState } from 'react'; import { apiFetch } from '@/lib/api'; import { getToken } from '@/lib/auth'; import { DataTable } from '@/components/DataTable'; export default function InventoryPage(){ const [rows,setRows]=useState<any[]>([]); useEffect(()=>{ apiFetch('/inventory',{},getToken()).then((r)=>setRows(r.data)); },[]); return <main><h1>Inventory & Spare Parts</h1><DataTable columns={['SKU','Item','Quantity','Min Qty','Unit Cost']} rows={rows.map((item)=>[item.sku,item.name,item.quantity,item.minQuantity,item.unitCost])} /></main>; }
