'use client'; import { useEffect, useState } from 'react'; import { apiFetch } from '@/lib/api'; import { getToken } from '@/lib/auth'; import { DataTable } from '@/components/DataTable'; export default function RequestsPage(){ const [rows,setRows]=useState<any[]>([]); useEffect(()=>{ apiFetch('/requests',{},getToken()).then((r)=>setRows(r.data)); },[]); return <main><h1>Maintenance Requests</h1><DataTable columns={['Request No','Title','Status','Priority','Assigned','Tags']} rows={rows.map((item)=>[item.requestNo,item.title,item.status,item.priority,item.assignedTo?.name || 'Unassigned',item.tags?.map((t:any)=>t.tag.name).join(', ') || '-'])} /></main>; }
