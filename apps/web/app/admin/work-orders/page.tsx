'use client'; import { useEffect, useState } from 'react'; import { apiFetch } from '@/lib/api'; import { getToken } from '@/lib/auth'; import { DataTable } from '@/components/DataTable'; export default function WorkOrdersPage(){ const [rows,setRows]=useState<any[]>([]); useEffect(()=>{ apiFetch('/work-orders',{},getToken()).then((r)=>setRows(r.data)); },[]); return <main><h1>Work Orders & Scheduling</h1><DataTable columns={['WO No','Title','Status','Technician','Scheduled Start','Estimated Cost']} rows={rows.map((item)=>[item.workOrderNo,item.title,item.status,item.technician?.name || 'Not assigned',item.scheduledStart ? new Date(item.scheduledStart).toLocaleString() : '-',item.estimatedCost])} /></main>; }
