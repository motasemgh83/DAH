import { Nav } from '@/components/Nav'; export default function AdminLayout({ children }:{ children:React.ReactNode }){ return <div className="layout"><Nav /><div className="content">{children}</div></div>; }
