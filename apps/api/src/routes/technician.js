const router=require('express').Router(); const c=require('../controllers/technicianController'); const { authRequired, requirePermission }=require('../middleware/auth'); router.get('/overview', authRequired, requirePermission('dashboard:view'), c.technicianOverview); router.post('/normalize', authRequired, requirePermission('dashboard:view'), c.normalizeArabicPayload); module.exports=router;
