const router=require('express').Router(); const c=require('../controllers/settingsController'); const { authRequired, requirePermission }=require('../middleware/auth'); const audit=require('../middleware/audit'); router.get('/company', authRequired, requirePermission('settings:view'), c.getCompanySettings); router.put('/company', authRequired, requirePermission('settings:update'), audit('UPDATE_SETTINGS','CompanySetting'), c.updateCompanySettings); router.get('/calendar', authRequired, requirePermission('settings:view'), c.getCalendarSettings); module.exports=router;
