const router=require('express').Router(); const c=require('../controllers/reportController'); const { authRequired, requirePermission }=require('../middleware/auth'); router.get('/dashboard', authRequired, requirePermission('reports:view'), c.dashboardReport); router.get('/kpis', authRequired, requirePermission('reports:view'), c.kpiReport); router.get('/costs', authRequired, requirePermission('reports:view'), c.costReport); module.exports=router;
