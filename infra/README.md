# Infrastructure Notes

This repository ships with Dockerfiles and docker-compose for local deployment.
For production, add a reverse proxy, secrets management and persistent object storage for attachments.
